[providers]
python = "3.11"

[phases.setup]
nixPkgs = [
  "python311",
  "python311Packages.pip",
  "gcc",
  "gfortran",
  "eccodes",
  "netcdf",
  "zlib",
  "libpng"
]

[phases.install]
cmds = ["echo 'Skipping default pip install'"]

[phases.build]
cmds = ["echo 'Custom build handled in start.sh'"]

[phases.start]
cmd = "./start.sh"
